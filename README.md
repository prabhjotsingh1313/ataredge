# AEA Tutoring (minimal demo)

A small Express/EJS app with local signup/login and tutor profiles.

Quick start

```bash
cd AEA
npm install
npm start
# open http://localhost:3000
```

Notes

- Uses SQLite database stored at `data/data.db`.
- For production, use a stronger session secret and secure session store.
